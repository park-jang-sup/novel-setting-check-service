# 다음 세션 작업 재개 프롬프트 — novel-setting-check 서비스 (MVP)

## 현재 상태 (이 프롬프트 작성일: 2026-09-12)

### 저장소 상태
- 위치: `C:\novel-setting-check-service`
- 브랜치: `main`
- origin/main보다 4커밋 앞섬 (로컬 전용, 아직 push 안 함)
- 마지막 커밋: `4163c1a` — test 원고/설정집 추가
- 작업 트리: clean

### 커밋 로그 (최근 4개)
```
4163c1a test: 검사 대상 원고/설정집 추가 (원고 12,263자, 인물 7명, 지명 3개, 시간선 5개)
d5a1dcf docs: PRD 6) 실측 값으로 수정 (원고 12,263자, violations 10건, proposed 311건, age_conflict/timeline_reverse 실측 근거 반영)
dd64f48 docs: PRD.md 실측 결과 반영 — A-0 검증 기반 수정
b0d886f docs: PRD.md 추가 — novel-setting-check 서비스 MVP 설계
```

### 등록된 스킬
- `skill-to-service` — 스킬을 Vercel 웹 서비스로 바꾸는 워크플로우
- `novel-setting-check` — 설정집+원고 대조 검사 스킬 (스크립트: `skills/novel-setting-check/scripts/check_setting.py`)

---

## 지금까지 한 일 요약

### 1. 스킬 2개 등록 완료
- `skill-to-service`: 설명 길이 반복 조정 끝에 등록 성공. 호출 예시: "스킬을 서비스로 만들고 싶다" / "PRD로 정리해줘" / "구현해줘"
- `novel-setting-check`: 설명 60자 이하로 조정해 등록 성공. 호출 예시: "설정 오류 좀 찾아줘" / "원고랑 설정집 대조해줘" / "개연성 확인해줘"
- `hermes skills list` 결과: "0 hub-installed, 51 builtin, 2 local — 53 enabled, 0 disabled" 확인 완료

### 2. A-1 설계 검토 완료
- WHO/WHEN/INPUT/OUTPUT/SETUP/SOLVE 5개 걸음 검토
- 충돌 3건 지적 후 사용자 판단으로 흐름 확정:
  - INPUT: 설정집 필수는 서비스가 대신 처리 (1화 작성→설정집 비어있음→추가제안→승인→설정집 생성)
  - SETUP 비밀: UPSTAGE_API_KEY는 설정집 생성 시에만 Solar 1회 호출, 회차 검사·승인 버튼은 호출 0
  - SETUP 프로필: 설정집 → localStorage 저장 + 초기화, 자동반영 없음, 승인 후 반영

### 3. PRD 작성 및 수정 완료 (docs/PRD.md)
- 11개 섹션 미니 PRD + 대응표 + 필드명 표
- 여러 번 수정 거쳐 최종 확정
- A-0 실측 결과 반영 (dd64f48)
- PRD 6) 실측 값 재수정 완료 (d5a1dcf)

### 4. A-0 검사 실행 및 실측 결과 확보

#### 실행 명령어
```bash
py -3 "C:/novel-setting-check-service/skills/novel-setting-check/scripts/check_setting.py" "C:/novel-setting-check-service/test/settings.md" "C:/novel-setting-check-service/test/manuscript.txt"
```

#### 실측 결과 (최신 run)
- **원고**: 12,263자
- **설정집**: 인물 7명, 지명 3개, 시간선 5개
- **violations**: 10건, 전부 name_variant(medium), 전부 오탐
  - 이번 예: 이어진/이미 → 이무진, 감정 → 김수정, 저녁/전혀 → 진혁, 관문 → 광화문, 심정 → 수정, 성분 → 서울, 물건 → 박무건, 김태진 → 김진규
- **proposed_additions**: 311건, 대부분 일반어·고빈도어 노이즈
- **not_checked**: []
- **errors**: []

#### 실측 상세 (PRD 6)에 기록됨)
1. **age_conflict 미검출**: 설정집 유진혁 나이 19. 원고에 나이 표현 2개 있음.
   - 라인 3 "만 19살 성인이 되는 날" → 값 19 (설정과 같음, skip)
   - 라인 14 "스무 살 인생에 혹시라도" → 값 20 (설정과 다름, 충돌 가능성)
   - 스크립트 로직: 나이 표현을 find_ages로 찾고, 그 표현이 인물명/별칭과 같은 윈도우(앞 60자 + 뒤 20자) 안에 있을 때만 age_conflict로 잡음. 라인 14의 "스무 살" 앞뒤 60+20자 윈도우에 '유진혁'도 '진혁'도 없음 → 충돌 미검출
   - 즉 나이 표현과 인물명이 함께 나온 문장만 대조하므로, 인물명 없이 나이만 나오는 서술은 대조를 못함. 사용자는 이 상태를 알 수 없음 → 명백히 잘못됨

2. **timeline_reverse 미검출**: 설정집 시간선 5개의 원고 등장 횟수
   - 수능: 2회, 라인 3
   - 탑 등장: 0회
   - 튜토리얼: 4회, 라인 3·5·7·9
   - 불심검문: 1회, 라인 5
   - 베나토르 합류: 0회
   - 스크립트: 본문에 있는 사건만 모아 순서 역행 확인. 0회인 두 건(탑 등장, 베나토르 합류)은 대조 대상 아님. 남은 3개 중 2건 이상 matched 조건이라 한 건을 못 읽었다는 사실이 드러나지 않음. not_checked에도 안 들어감.

3. **name_variant 오탐**: 일반 명사를 인명 오타로 판정. 오타와 그냥 다른 이름 구분 못 함.

4. ** proposed_additions 노이즈**: 311건 대부분 일반어·고빈도어. 단어 빈도/표기 유사도만으로는 의미 있는 후보 추출 어려움.

5. **심은 오류 미검출**: 스킬 이름 "쾌속"→"질주" 변경 지점. 검출 5종에 "회차 간 인물-능력/용어 표기 변동" 유형 없어서 못 잡음. 추가/수정 필요.

### 5. 커밋 완료
- `b0d886f` PRD.md 최초 추가
- `dd64f48` PRD 실측 결과 반영
- `d5a1dcf` PRD 6) 실측 값 재수정
- `4163c1a` test 원고/설정집 추가 (manuscript.txt, settings.md, output.json — 3 files, 2344 insertions)

---

## 검사 결과 요약 — 다음 작업 판단용

### 검출 5종 현황
| 유형 | 이번 결과 | 상태 |
|------|-----------|------|
| age_conflict | 0건 | 원고에 나이 표현 있는데 인물명 윈도우에 없어서 미검출. 실제 충돌 가능성(19 vs 20) 있음. 잘못됨 |
| ability_candidate | 0건 | 불가 키워드(마법) + 인물명 윈도우 조건. 키워드-인물명 인접 안 해서 0건 |
| timeline_reverse | 0건 | 0회인 시간선 2개 때문에 못 읽은 사실 안 드러남 |
| name_variant | 10건 (전부 오탐) | 오탐률 100%. 일반어-인명 구분 못 함 |
| proposed_additions | 311건 | 대부분 노이즈. 실제 추가 후보는 적음 |

### 검출 한계 요약
- 규칙 기반 스크립트의 구조적 한계: 인물명/별칭 윈도우 안에 있는 표현만 대조. 서술형 나이 표현, 일반어-고유명사 구분, 회차 간 표기 변동 감지는 못 함.
- Solar 1회 호출(P0-1/P0-5)은 이 한계를 보완하기 위한 것. 분류·제안 단계에서 LLM 판단으로 노이즈 걸러내고 실제 후보 추리는 역할.
- 설계원칙 4개:
  1. 검사하지 못한 것을 문제없다고 말하지 않는다.
  2. 설정집을 임의로 수정하지 않는다.
  3. 스크립트가 찾지 않은 오류를 지어내지 않는다.
  4. 원고를 직접 읽어 판단하지 않는다.

---

## 다음 세션 진행 후보

### 선택지 A: A-0 재료 명세서 정리 (A-정리)
- A-0에서 확인한 내용을 표로 정리. 입력/반환/실패방식/필요한설정/이 스킬만으로 못하는 일 3가지.
- PRD 6)에 이미 상세히 적혀 있어서, 이걸 표 형태로 추출·정리하는 작업.
- 맛보기 구조:
  | 항목 | 내용 |
  |------|------|
  | 입력 | 설정집(md) + 원고(txt) |
  | 반환 | JSON (summary/violations/proposed_additions/not_checked/errors) |
  | 실패 방식 | 파일 없음·인코딩 오류·빈 원고·형식 오류 시 exit(1) + JSON 오류 응답 |
  | 필요한 설정 | 없음(스크립트 자체). 단 서비스에서는 API_KEY가 P0-5 단계에서 필요 |
  | 이 스킬만으로 못하는 일 3가지 | ① 인물명 없이 나오는 나이 표현 대조 불가 ② 일반어-고유명사 구분 불가 ③ 회차 간 표기 변동 감지 불가 |

### 선택지 B: PRD 확정 및 구현 단계(C)로
- PRD 이미 충분히 작성됨. A-0 실측도 확보됨.
- skill-to-service 워크플로우(A-0→A-1→A-2→B→C)에 따라 구현으로 넘어갈 수 있음.
- 단, A-0 결과상 스크립트 한계가 명확하므로, 구현 전에 "이 한계를 서비스 설계에서 어떻게 보완할지"를 PRD/C 단계 설계에 반영해야 함.

### 선택지 C: 검출 개선 논의
- 현재 검출 5종 그대로 유지하되, Solar 분류 단계에서 보완할 방법.
- 또는 스크립트에 추가할 검사 유형 검토(회차 간 표기 변동 등).
- PRD에 "추가/수정 필요"로 적혀 있는 부분을 실제 설계로 옮길지 결정.

### 선택지 D: 나이 표현 인식 개선 (실측 기반)
- find_ages가 "21세기" 같은 비나이 표현을 나이로 잡음(라인 9 "21세기 세상에서" → 값 21). 현재 윈도우에 인물명이 없으면 age_conflict는 안 잡히지만, 인물명 근처에 있으면 오탐 가능성 있음.
- "스무 살"(라인 14, 값 20)은 스크립트가 잡을 수 있는 표현인데, 윈도우(앞 60+뒤 20)에 인물명이 없어서 age_conflict 미검출. 인물명 근처였다면 충돌로 잡혔을 구조.
- 요청 표현 중 실제 등장: "스무 살" 1건. 나머지("스무 살이었다", "17살", "17살이었다", "20세", "20세였다", "열입곱 살", "열입골 살이었다", "스물세 살", "19세기", "5세대")는 원고에 없음.
- 개선 방향 후보: 나이 표현 판정 시 주변 문맥(동아시아 나이/세기/세대 등) 걸러내기, 인물명이 멀리 있어도 화자 추정 가능한 서술형 문장에서의 나이 표현은 별도 처리 등. 단 이건 구현 단계(C)에서 구체적 설계 필요.

---

## 규칙 (항상 지킬 것)

1. 파일 생성·수정·삭제, 배포, 외부 호출은 전부 승인받고 진행
2. 승인 없이 실행한 게 있으면 바로 알릴 것
3. 단계 끝날 때마다 "뭘 했다"가 아니라 실행 출력을 그대로 붙일 것

## 실행 환경 참고
- Windows 환경, git-bash에서 명령 실행
- Python 실행: `py -3` 사용 (`python`/`python3` 직접 실행 시 권한 오류)
- 한글 파일명은 영문으로 저장 (인코딩 이슈 방지)

## 주요 파일 경로
- PRD: `C:\novel-setting-check-service\docs\PRD.md`
- 검사 스크립트: `C:\novel-setting-check-service\skills\novel-setting-check\scripts\check_setting.py`
- 스킬 정의: `C:\novel-setting-check-service\skills\novel-setting-check\SKILL.md`
- 검사 대상 설정집: `C:\novel-setting-check-service\test\settings.md`
- 검사 대상 원고: `C:\novel-setting-check-service\test\manuscript.txt`
- 이 프롬프트: `C:\novel-setting-check-service\NEXT_SESSION_PROMPT.md`

## 추정치 금지
- 없는 값은 "확인 불가"로 둔다.
- 추측하지 말고 직접 확인(예: grep, 스크립트 실행, 파일 읽기) 후 판단한다.
- 검출 결과는 스크립트 JSON 출력만 근거로 삼는다.

---
프롬프트 끝.

### find_ages 실측 결과 (2026-09-12, 오타 수정 반영, 다음 세션 개선 대상)

#### 1) find_ages 직접 투입 결과 (요청 표현 12개 기준)
요청 표현 12개: 스무 살이었다 / 17살 / 17살이었다 / 20세 / 20세였다 / 열일곱 살 / 열일곱 살이었다 / 스물세 살 / 19세기 / 5세대

직접 투입 결과(find_ages에 표현만 넣음):
- 미인식: 스무 살이었다 / 17살이었다 / 20세였다 / 열일곱 살이었다
- 인식(값 반환):
  - 17살 → 17 (정확)
  - 20세 → 20 (정확)
  - 열일곱 살 → 17 (정확)
  - 스물세 살 → 20 (오인식, 기대 23)
  - 19세기 → 19 (오인식, 세기가 아님)
  - 5세대 → 5 (오인식, 세대가 아님)

요청 표현 12개 중 정확히 인식한 건 4개(17살, 20세, 열일곱 살, 그리고 실제 원고에는 있었지만 요청 목록엔 없던 19살). 나머지는 미인식이거나 오인식.

#### 2) 정규식 패턴 문제 (세기/세대는 못 막고 ~이었다만 막음)
- find_ages 정규식은 살|세|해를 나이 접미사로 본다.
- 그런데 "19세기", "5세대" 같은 표현이 살|세|해 패턴과 충돌해서 나이로 잡힌다.
- 반대로 "~이었다" 형태는 현재 "_이었다"가 붙으면 아예 매칭이 안 되는 구조다. 즉 나이 표현 뒤에 "이었다"가 오면 인식하지 못한다.
- 결과: 세기/세대는 못 막고, ~이었다만 막혀서 반대로 작동하고 있다.

#### 3) 살/세/해 구분자 문제 (나이 산수 오류)
- find_ages는 살|세|해를 나이 접미사로 쓰는데, 한국어에서 "세"는 나이 단위이기도 하고 일단위 수사이기도 하다.
  - 스물**세** 살, 서른**세** 살, 마흔**세** 살 처럼 "세"가 수사를 이루는 경우
  - 이때 "살|세|해"로 잘라버리면 "스물세 살"에서 "스물"만 남거나, 뒷부분 "세"가 분리되어 korean_age_to_int에 들어가는 구조가 깨진다.
- 실측 예:
  - 스물세 살 → 20 (기대 23)
  - 서른세 살 → 30 (기대 33)
  - 마흔세 살 → 40 (기대 43)
- 즉 "살/세/해"를 단순 접미사로 처리하면 일단위 수사가 붙은 나이에서 산수가 틀린다.

#### 4) 원고 vs 설정집 나이 (실측 요약)
- 원고: 12,263자 / 설정집 나이: 유진혁 19, 김수정 19
- find_ages가 원고에서 잡은 나이 표현:
  - 19살 (라인 3, 값 19)
  - 21세기 (라인 9, 값 21, 오인식)
  - 스무 살 (라인 14, 값 20)
- age_conflict 조건: 나이 표현 앞뒤 윈도우(앞 60 + 뒤 20)에 인물명/별칭이 있어야 검출
  - 위 세 표현 모두 윈도우에 진혁/유진혁이 없어서 age_conflict 미검출
- 충돌 여부:
  - 설정집 19 vs '스무 살'(20) → 값은 다름. 단 윈도우에 인물명이 없어 검출 안 됨
  - '스무 살이었다' → find_ages가 미인식 → 이 경우도 미검출
  - '19세기', '5세대' → 오인식 → 인물명 근처에 있으면 age_conflict 오탐 가능
  - '스물세 살' → 20으로 오인식 → 기대값 23, 실제 검출 시 20으로 잡힘

#### 5) 다음 세션에 고칠 두 가지
- (가) 세기/세대 필터링 + "~이었다" 처리
  - "세기", "세대"처럼 나이 단위가 아닌 접미사/단위가 붙은 표현은 나이 후보에서 제외
  - "~이었다" 형태의 나이 표현도 인식하도록 정규식 또는 전처리 수정
  - 목표: 요청 표현 12개 중 정확한 인식 개수를 4개에서 늘리기
- (나) 살/세/해 구분자로 인한 나이 산수 오류 수정
  - "살|세|해"를 단순 접미사로 잘라내는 방식 대신, 일단위 수사(스물세·서른세·마흔세 등)가 포함된 나이는 제대로 파싱하도록 수정
  - 목표: 스물세 살=23, 서른세 살=33, 마흔세 살=43처럼 맞게 반환
- 위 두 가지를 고친 뒤, 요청 표현 12개(및 추가 표현)로 재특정해서 결과를 다시 확인한다.

### 임시 스크립트 정리 상태 (2026-09-12)
- 삭제된 스크립트: _direct_find_ages_test.py, _age_check.py, _age_final_test.py, _find_ages_expr_test.py, _find_ages_expr_test2.py
- 남은 스크립트: _find_ages_expr_test3.py (find_ages 직접 투입 결과), _prompt_section.py (섹션 작성용)
- NEXT_SESSION_PROMPT.md에 find_ages 실측 결과 섹션 추가할 때는 이 두 스크립트 내용은 참고하되, 커밋 전 삭제한다.